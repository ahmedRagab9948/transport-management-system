# Frontend Features

Feature-based modules live here. Each feature may include:

- `components/` — feature UI
- `hooks/` — feature hooks
- `services/` — feature API calls
- `schemas/` — Zod schemas
- `types/` — feature types

Examples: `trips/`, `vehicles/`, `drivers/`
